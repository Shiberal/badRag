
# Running in dev mode

To run the FastAPI in dev mode, use the following command:

fastapi dev api.py 

main.py actually does nothing. the db does not currently get created automatically, and is not optimized for embeddings search.